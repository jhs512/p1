# 0002 Java 자료형 (Data Types) — 영상 설계 스펙

## 개요

Java 기초 쇼츠 시리즈 두 번째 영상. 0001(변수)에서 `int age = 25;`를 배운 뒤 자연스럽게 이어지는 내용으로, 변수에 넣을 수 있는 **데이터의 종류(자료형)**를 설명한다.

- 파일: `src/compositions/0002-JavaDataTypes.tsx`
- 오디오 설정: `src/compositions/0002-audio.ts` (sync 자동 생성)
- 해상도: 1080×1920 (쇼츠)
- 아웃트로 씬: **없음** (summaryScene이 마무리 역할)
- 예상 길이: ~44초 (크로스페이드 20프레임×6컷 겹침 반영)

---

## 씬 구성

| 씬 키 | 내용 | 색상 | 씬 길이 |
|---|---|---|---|
| `thumbnail` | 정지 화면 (유튜브 자동 썸네일) | — | 30프레임 고정 (나레이션 없음) |
| `intro` | 자료형 개념 소개 + 4개 상자 동시 등장 | 멀티컬러 | ~7s |
| `intScene` | int — 정수형 설명 + 코드 예시 | 파랑 `#4e9cd5` | ~8s |
| `doubleScene` | double — 실수형 설명 + 코드 예시 | 노랑 `#d4c04e` | ~8s |
| `stringScene` | String — 문자열 설명 + 코드 예시 | 초록 `#4ec970` | ~8s |
| `booleanScene` | boolean — 참/거짓 설명 + 토글 애니 | 주황 `#d4834e` | ~8s |
| `summaryScene` | 4개 타입 코드 한 화면 리뷰 | 멀티컬러 | ~8s |

thumbnail은 나레이션이 없으므로 `audio` 키를 갖지 않고 `durationInFrames: 30`을 VIDEO_CONFIG에 직접 지정한다.

---

## 오디오 파일 네이밍

0001 패턴(`scene0.mp3` ... `scene3.mp3`)과 충돌하지 않도록 prefix `dt-` 사용.

```
public/dt-intro.mp3
public/dt-int.mp3
public/dt-double.mp3
public/dt-string.mp3
public/dt-boolean.mp3
public/dt-summary.mp3
```

VIDEO_CONFIG 키와 오디오 파일 매핑:

| VIDEO_CONFIG 키 | audio 파일 | AUDIO_CONFIG 키 |
|---|---|---|
| `intro` | `dt-intro.mp3` | `intro` |
| `intScene` | `dt-int.mp3` | `intScene` |
| `doubleScene` | `dt-double.mp3` | `doubleScene` |
| `stringScene` | `dt-string.mp3` | `stringScene` |
| `booleanScene` | `dt-boolean.mp3` | `booleanScene` |
| `summaryScene` | `dt-summary.mp3` | `summaryScene` |

---

## 나레이션 (초안)

```ts
intro: [
  "자료형이란 변수에 저장할 수 있는 데이터의 종류입니다.",
  "Java에는 네 가지 핵심 자료형이 있습니다.",
],
intScene: [
  "int는 정수를 저장하는 자료형입니다.",
  "나이나 개수처럼 소수점이 없는 숫자에 사용합니다.",
],
doubleScene: [
  "double은 소수점이 있는 실수를 저장합니다.",
  "키나 무게처럼 정밀한 값이 필요할 때 사용합니다.",
],
stringScene: [
  "String은 문자열을 저장합니다.",
  "이름이나 메시지처럼 텍스트를 담을 때 사용합니다.",
],
booleanScene: [
  "boolean은 참 또는 거짓만 저장하는 자료형입니다.",
  "조건 검사 결과를 담을 때 사용합니다.",
],
summaryScene: [
  "네 가지 자료형을 코드로 정리해봤습니다.",
  "상황에 맞는 자료형을 선택하는 것이 중요합니다.",
],
```

---

## 코드 예시

```java
int age = 25;
double height = 175.5;
String name = "Java";
boolean isStudent = true;
```

타입 씬에서 해당 줄만 `isNew: true`로 타이핑 애니메이션.
summaryScene에서 4줄 순차 타이핑 (아래 summaryScene 항목 참조).

---

## 시각 디자인

### 상자 색상 팔레트

| 타입 | 상자 색 | 값 예시 |
|---|---|---|
| int | `#4e9cd5` (파랑) | `25` |
| double | `#d4c04e` (노랑) | `175.5` |
| String | `#4ec970` (초록) | `"Java"` |
| boolean | `#d4834e` (주황) | `true` |

### 인트로 씬
- 4개 상자가 화면 중앙에 2×2 그리드로 stagger spring 등장 (0, 5, 10, 15프레임 딜레이)
- 각 상자 안에 타입명 표시

### 타입 씬 공통 패턴 (4씬 동일)
1. `TypeBox` spring 등장 (화면 중앙)
2. 코드 타이핑 (`int age = 25;` 등) — `CodeBox` 재사용
3. 값 낙하 애니메이션 (TypeBox 안으로 값이 위에서 내려옴)
4. 자막 2문장

### boolean 씬 특수 애니메이션
- TypeBox 안에서 `true` ↔ `false` 토글 (1.5초 주기, `interpolate` 기반)
- true 색: `#4ec9b0`, false 색: `#f44747`

### summaryScene
- 4줄 코드를 순차적으로 타이핑: 4개의 `<Sequence>`를 사용하며, 각 Sequence는 누적 `CodeLine[]`을 `<CodeBox>`에 넘긴다. 이전 줄은 `isNew: false`, 현재 타이핑 줄은 `isNew: true`.
- 각 줄 타입 키워드에 해당 타입 색상 적용 (`ColorizedCode` 사용)

---

## 컴포넌트 설계

0001과 마찬가지로 **모든 컴포넌트는 `0002-JavaDataTypes.tsx` 파일 내에 인라인으로 정의**한다. 0001의 컴포넌트는 임포트하지 않고, 동일한 패턴을 파일 내에서 재구현한다.

### 복사 (0001과 동일 패턴)
- `useTypingEffect` — 변경 없음
- `SceneTitle` — 변경 없음
- `StaticLine` / `TypingLine` / `CodeBox` — 변경 없음
- `Subtitle` — 변경 없음

### 신규 또는 재설계

#### `ColorizedCode` (0002 버전)
0001의 `ColorizedCode`는 모든 타입 키워드를 단일 색(`#4ec9b0`)으로 표시했다. 0002에서는 **타입 키워드마다 고유 색상**을 사용하도록 재설계한다.

키워드 → 색상 매핑:
| 키워드 | 색상 |
|---|---|
| `int` | `#4e9cd5` (파랑) |
| `double` | `#d4c04e` (노랑) |
| `String` | `#4ec970` (초록) |
| `boolean` | `#d4834e` (주황) |
| 숫자 리터럴 (`\d`) | `#b5cea8` (연두, 기존 유지) |
| 문자열 리터럴 (`"..."`) | `#ce9178` (주황계, 기존 유지) |

구현: `KEYWORD_COLORS` 맵 + 정규식 분리 → `parts.map((part) => KEYWORD_COLORS[part] ? <span color={KEYWORD_COLORS[part]}> : ...)`

#### `TypeBox`
색상(`color: string`)과 값(`value: string`)을 받는 범용 변수 상자.
- 상자가 spring으로 등장 (scale 0 → 1)
- 지정 프레임 이후 값이 위에서 낙하 (translateY + opacity, 0001의 BoxMetaphorAnim 낙하 로직 기반)
- Props: `color`, `value`, `startFrame`, `dropStartFrame`

#### `BooleanToggleAnim`
`TypeBox`를 래핑한 boolean 전용 컴포넌트.
- TypeBox 내부 값이 `interpolate`로 `true` ↔ `false` 토글 (45프레임 주기)
- true 색: `#4ec9b0`, false 색: `#f44747`

---

## 파일 구조

```
src/compositions/
  0002-JavaDataTypes.tsx   ← 메인 컴포지션 (모든 컴포넌트 인라인)
  0002-audio.ts            ← AUTO-GENERATED by pnpm sync 0002
public/
  dt-intro.mp3             ← AUTO-GENERATED
  dt-int.mp3
  dt-double.mp3
  dt-string.mp3
  dt-boolean.mp3
  dt-summary.mp3
```

---

## 구현 순서

1. `0002-JavaDataTypes.tsx` 골격 생성 (VIDEO_CONFIG + 씬 목록 + 빈 씬 컴포넌트 + `import { AUDIO_CONFIG } from "./0002-audio"`)
   - `sync.ts`가 실행 전에는 `0002-audio.ts`가 없으므로, tsc는 step 10까지 실행하지 않는다.
2. `pnpm sync 0002` 실행 → `0002-audio.ts` 스텁 자동 생성 → TTS mp3 생성 → 실측 duration → narrationSplits → `0002-audio.ts` 완성
3. `TypeBox` 컴포넌트 구현 (color/value 파라미터, spring 등장, 값 낙하 애니메이션)
4. `ColorizedCode` 구현 (per-keyword 색상 맵, `double` 포함)
5. `IntroScene` 구현 (4개 상자 stagger 등장)
6. `intScene`, `doubleScene`, `stringScene` 구현 (TypeBox + CodeBox 조합)
7. `BooleanToggleAnim` 구현 → `booleanScene` 구현
8. `summaryScene` 구현 (4 Sequence × CodeBox 누적 + ColorizedCode 키워드 색)
9. `src/Root.tsx`에 0002 컴포지션 등록
10. 타입 체크 (`npx tsc --noEmit`) + Remotion Studio 미리보기 확인
11. 커밋
